@model UserDto
@{
}

<div class="row">
	<div class="col-md-8 mx-auto rounded border p-3">
		<h2 class="text-center mb-5">Kullanıcı Düzenle</h2>

		<form method="post">
			<div class="row mb-3">
				<label class="col-sm-4 col-form-label">Id</label>
				<div class="col-sm-8">
					<input class="form-control-plaintext" readonly value="@ViewData["UserId"]" />
				</div>
			</div>

			<div class="row mb-3">
				<label class="col-sm-4 col-form-label">Username</label>
				<div class="col-sm-8">
					<input class="form-control" asp-for="Username" />
					<span asp-validation-for="Username" class="text-danger"></span>
				</div>
			</div>

			<div class="row mb-3">
				<label class="col-sm-4 col-form-label">WorkingHours</label>
				<div class="col-sm-8">
					<input class="form-control" asp-for="Password" />
					<span asp-validation-for="Password" class="text-danger"></span>
				</div>
			</div>

			<div class="row mb-3">
				<label class="col-sm-4 col-form-label">Appointment Price</label>
				<div class="col-sm-8">
					<input class="form-control" asp-for="Password" />
					<span asp-validation-for="Password" class="text-danger"></span>
				</div>
			</div>

			<div class="form-group">
				<label>Aktiflik Durumu:</label>
				<div>
					<input type="radio" id="available" name="IsAvailable" value="true" @(Model.IsActive ? "checked" : "") />
					<label for="available">Aktif</label>
				</div>
				<div>
					<input type="radio" id="notAvailable" name="IsAvailable" value="false" @(!Model.IsActive ? "checked" : "") />
					<label for="notAvailable">Aktif değil</label>
				</div>
			</div>

			<div class="row">
				<div class="offset-sm-4 col-sm-4 d-grid">
					<button type="submit" class="btn btn-primary">Submit</button>
				</div>
				<div class="col-sm-4 d-grid">
					<a class="btn btn-outline-primary" asp-controller="Admin" asp-action="SalonDashboard" role="button">Cancel</a>
				</div>
			</div>

		</form>
	</div>
</div>